import styled from 'styled-components';

const ScreenWrapperStyles = styled.div`
    background: linear-gradient(
        to right,
        rgba(3, 67, 120, 1) 0%,
        rgba(35, 125, 199, 1) 100%
    );
`;

export default ScreenWrapperStyles;
